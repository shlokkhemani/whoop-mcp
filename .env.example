# WHOOP Server Environment Variables
# Copy this file to .env and replace placeholder values.
# All values are safe to keep in local development, but never commit your filled-in .env.

# ------------------------------------------------------------------
# Required WHOOP app credentials (same for local and hosted setups)
# ------------------------------------------------------------------
WHOOP_CLIENT_ID=your_client_id_here
WHOOP_CLIENT_SECRET=your_client_secret_here

# ------------------------------------------------------------------
# MCP server base URL
#   Local development: keep the default localhost URL below.
#   Hosted deployment: comment out the localhost line and provide your public HTTPS URL. (recommend fastmcp.app)
# ------------------------------------------------------------------
PUBLIC_BASE_URL=http://localhost:9000
# PUBLIC_BASE_URL=https://your-mcp.fastmcp.app/ 

# Path the OAuth proxy should use for the redirect callback. This must
# match the redirect configured in the WHOOP developer portal.
OAUTH_REDIRECT_PATH=/auth/callback

